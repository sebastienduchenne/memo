utilisation ressources
    htop
--------------------------------------------------------------------
Installer un paquet deb
    sudo dpkg -i package_file.deb
--------------------------------------------------------------------
set PATH environment
    export PATH=$PATH:/place/with/the/file
--------------------------------------------------------------------
afficher les ports ouverts
    netstat -ln
--------------------------------------------------------------------
afficher les processus actifs
    pstree
    pstree -p | grep chrome
    pkill -e chrome / chromedriver / geckodriver
--------------------------------------------------------------------
connexion SSH
    ssh -L 8042:localhost:8042 -L 8080:localhost:8080 sduchenne@10.107.3.101
--------------------------------------------------------------------
Pointer sur un site

sudo /etc/hosts

#site
ip1 domaine1
ip2 domaine1
--------------------------------------------------------------------
requête POST avec header
    curl -X POST url -H "header: value" --data 'value'
--------------------------------------------------------------------
chmod : modifier les permissions d’un fichier ou d’un dossier
wget
--------------------------------------------------------------------
### dossier et fichier

supprimer dossier :     rm -rf nomDir
compresser fichier :    tar cvf archive.tar.gz dossier
decompresser fichier :  tar xvf archive.tar
transfert fichier :     scp 
