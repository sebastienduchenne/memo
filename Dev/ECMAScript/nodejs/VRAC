-export NODE_ENV=production : indiquer à node que l'app est lancée en production
-toute ce qui est créée est par défaut local au module et non global comme en JS
-éviter de bloquer le thread par opération synchrone ou des boucle infini
