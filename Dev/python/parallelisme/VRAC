-module multiprocessing
    -créer des processus
    -mémoire partagée
    -multicoeur
-classe qthread
    -classe de PyQt qui enrobe l'exécution d'un thread, mais ne fait pas de vrais threads. 
    -permet aussi d'envoyer des signaux entre les threads
    -on peut arrêter "sauvagement" un QThread avec terminate()
    -parallélisme sur 1 coeur à cause du Global Interpreter Lock (GIL) dans CPython
-module Threading de Python
    -on ne peut pas arrêter "sauvagement" un thread
-utilisation notamment avec des GUI pour faire de gros calculs
-les threads s'exécutent sur un seul coeur à cause du GIL (Global Interpreter Lock) de cPython
