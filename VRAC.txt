-algorithme hyperloglog = probabiliste
-vector clock
-garbage collector
-développeur full stack = familiarisé avec les différentes couches d'une appli :
    -réseau et environnement hote
    -données
    -business logic
    -API
    -UI
    -UX
    -besoin du consommateur
-CDN = content delivery network ou réseau de diffusion de contenu (RDC)
-Firebase = ensemble de services d'hébergement d'applications
-yeoman
-scaffolding


*** transaction
-propriétés ACID
    -atomicité : la transaction se fait au complet ou pas du tout
    -cohérence : la transaction amènera le système d'un état valide à un autre état valide
    -isolation : pas de dépendance entre les transactions
    -durabilité : les résultats doivent être enregistrés de façon permanente


*** technologies
-cloudflare = CDN
-puppet = gestion de la configuration de serveurs esclaves
-NuGet = package manager