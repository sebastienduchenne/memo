CDN content delivery network : service diffusant du contenu et situé à des emplacements stratègiques pour délivrer le plus rapidement possible à un utilisateur
infrastructure réseau
cloud computing
load balancing
modèle OSI
routeur
port logiciel
couche transport TCP
adresse IP
adresse MAC
virtualisation
LAN
carte réseau
ethernet
outil : arp, ping, tracert/traceroute, ipconfig/ifconfig, netstat, nbtstat, nslookup
par-feu
proxy
DHCP
TCP/IP
protocole HTTP : HTTP/1.0, HTTP/1.1, HTTP/2
DNS

-------------------------------------------

*** port logiciel

port réseau est un peu comme une porte à laquelle l'application d'un équipement se connecte à un autre équipement distant par le réseau
socket ou interface de connexion
couche transport TCP
distingué par son numéro
sert localement à identifier un processus
comme des portes donnant accès au système d'exploitation 

Un numéro de port est codé sur 16 bits, ce qui fait qu'il existe un maximum de 2^6,soit 65 536 ports distincts par machine. 

Ces ports sont classés en 3 catégories en fonction de leur numéro : 
    de 0 à 1 023 : ports "bien-connus" (well-known ports), utilisés pour les services réseaux les plus courants.
    de 1 024 à 49 151 : ports enregistrés (registered ports), assignés par l'IANA
    de 49 152 à 65 535 : ports dynamiques, utilisables pour tout type de requêtes TCP ou UDP autres que celle citées précédemment.


Lorsqu'un équipement se connecte à un autre distant :
    Il ouvre un port réseau sur la machine, on parle de port local.
    Puis il se connecte au port d'un équipement distant, on parle alors de port distant.


Un port spécifique est identifié par :
    son numéro de port,
    l'adresse IP à laquelle il est associé,
    le protocole utilisé pour la communication (TCP ou UDP)
