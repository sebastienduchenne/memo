certificat électronique
    = comme une carte d'identité numérique
    -permet d'identifier une personne
    -contient : une clé publique, des infos d'identification, une signature
    -signé par un tier de confiance
