-API Window -> AbstractView
-Document -> Node -> EventTarget
-Element  ->
-FetchAPI : objet Request et Response


*** API
-Web storage API
    -sessionStorage
    -localStorage
-Web worker
    -worker dédié
    -worker partagé
    -service worker
    -worker chrome
    -audio worker
-API fetch : récupération de ressources
    -Request
    -Response
    -Headers
    -GlobalFetch
-Document
-IndexedDB
-API store data
-WebGL = web Graphic Library
-WebSocket = ouvrir un canal de communication bidirectionnelle entre un navigateur et un serveur
-Window
    -Console
-DOM
-Cache
-EventTarget : recevoir des évènements et avoir des auditeurs
-Event : représente tout événement qui a lieu dans le DOM (FetchEvent)
-Node
-Blob : représente un objet, semblable à un fichier, qui est immuable et qui contient des données brutes


*** Accès
sessionStorage
localStorage
ServiceWorker.active
